# 投稿用图由 make_submission_figures.py 从出图脚本的产物转出，可复现，不入库。
# 出图脚本自身的产物（Figure*_*.png/.pdf/.eps）仍然入库，便于比对与追溯。
*.tif
*.tiff
Fig1.eps
Fig2.eps
FigS1.eps
FigS2.eps

# Python 字节码缓存
__pycache__/
*.pyc

# ICMJE 表由 make_icmje_forms.py 从模板填出，可复现，不入库（模板本身入库）
ICMJE_Jun_Shu.docx
ICMJE_Kai_Zheng.docx
ICMJE_Hongqiang_Bian.docx
ICMJE_Jun_Yang.docx
ICMJE_Xin_Wang.docx

# 投稿图改为 PNG 后的产物（2026-09-11）
Fig1.png
Fig2.png
FigS1.png
FigS2.png
# Fig1.png 的像素数（11520x8880）超过对话文件通道对图片的上限，直接发会被退回
# 400，故打包后传给作者。压缩包同为可复现产物，不入库。
Fig1_png.zip

# 论文投稿审查评议 PPT（医院内部答辩用，与投稿包无关的一次性工作）：
# 模板文件与生成脚本均在此目录下，模板是用户上传的院内表单（13MB），
# 产出的 pptx 已直接发给用户，不必让这类大体积无关二进制文件进仓库历史。
ppt_work/
